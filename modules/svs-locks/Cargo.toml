[package]
name = "svs-locks"
version = "0.1.0"
edition = "2021"
description = "Time-lock module for SVS vault programs - lock periods before redemption"
license = "Apache-2.0"

[dependencies]

[dev-dependencies]
proptest = "1.4"
